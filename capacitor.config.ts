import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.explanation.vizz',
  appName: 'vizz',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
